# Drawing Automation

`drawing_auto.swp` is the runnable macro. In SolidWorks choose **Tools > Macro >
Run**, select the file, and test it on a disposable drawing before using a
production drawing or template.


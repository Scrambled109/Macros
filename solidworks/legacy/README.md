# Experimental SolidWorks Macros

`bounding-box-test/Bounding box.swp` and `bounding-box-test/Test_test.swp` are
retained because the user branch includes every `.swp` file. They are
experimental/older files, not approved production tools. Do not use them on
production models until their owner validates the behavior on a recoverable
copy.


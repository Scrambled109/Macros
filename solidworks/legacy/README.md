# Non-production and legacy material

Nothing in this directory was deleted. Files were moved here only where the
name or source makes their status explicit:

- `bounding-box-test/` contains the experimental `Test_test.swp` and the older
  minimally guarded `Bounding box.swp`.
- `drawing-automation/` contains a version-named source upload that is nearly
  identical to the current drawing automation export.

Do not promote or delete these files until their owner validates that decision.
